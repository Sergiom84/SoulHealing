import React from "react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { motion } from "framer-motion";
import { CheckCircle, Repeat, Dove, ScrollText } from "lucide-react";

function WelcomeScreen({ onContinue }: { onContinue: () => void }) {
  return (
    <div className="min-h-screen bg-white text-gray-800 flex flex-col items-center justify-center px-6">
      <motion.div
        className="text-center"
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ duration: 1 }}
      >
        <h1 className="text-4xl font-serif text-blue-900 mb-6">Bienvenido a la Rutina de Perdón</h1>
        <p className="text-lg text-gray-600 max-w-xl mb-8">
          “El perdón es la llave de la felicidad.”<br />
          <span className="text-sm italic">– Un Curso de Milagros</span>
        </p>
        <Button
          className="bg-blue-600 text-white px-6 py-3 rounded-2xl shadow hover:bg-blue-700 transition"
          onClick={onContinue}
        >
          Entrar a la app
        </Button>
      </motion.div>
    </div>
  );
}

function MainAppContent() {
  return (
    <div className="min-h-screen bg-white text-gray-800 p-6 flex flex-col items-center justify-center">
      <motion.h1
        className="text-4xl font-serif text-blue-900 mb-4"
        initial={{ opacity: 0, y: -20 }}
        animate={{ opacity: 1, y: 0 }}
      >
        Rutina de Perdón
      </motion.h1>

      <motion.p
        className="text-lg text-gray-600 mb-8 max-w-xl text-center"
        initial={{ opacity: 0 }}
        animate={{ opacity: 1 }}
        transition={{ delay: 0.3 }}
      >
        Una herramienta serena y poderosa para soltar, comprender y sanar desde el corazón.
      </motion.p>

      <div className="grid grid-cols-1 sm:grid-cols-2 gap-6 w-full max-w-4xl">
        <Card className="rounded-2xl shadow-md border border-blue-100">
          <CardContent className="p-6 flex flex-col items-center text-center">
            <CheckCircle className="text-blue-600 w-8 h-8 mb-2" />
            <h2 className="text-xl font-semibold text-blue-800 mb-2">Checkpoints</h2>
            <p className="text-gray-600">Revisa y sigue los pasos esenciales para aplicar el perdón.</p>
          </CardContent>
        </Card>

        <Card className="rounded-2xl shadow-md border border-blue-100">
          <CardContent className="p-6 flex flex-col items-center text-center">
            <Repeat className="text-blue-600 w-8 h-8 mb-2" />
            <h2 className="text-xl font-semibold text-blue-800 mb-2">Proceso</h2>
            <p className="text-gray-600">Accede al ciclo completo del perdón y retómalo donde lo dejaste.</p>
          </CardContent>
        </Card>

        <Card className="rounded-2xl shadow-md border border-blue-100">
          <CardContent className="p-6 flex flex-col items-center text-center">
            <Dove className="text-blue-600 w-8 h-8 mb-2" />
            <h2 className="text-xl font-semibold text-blue-800 mb-2">Liberación</h2>
            <p className="text-gray-600">Observa tu evolución y experimenta la paz que emerge con el perdón.</p>
          </CardContent>
        </Card>

        <Card className="rounded-2xl shadow-md border border-blue-100">
          <CardContent className="p-6 flex flex-col items-center text-center">
            <ScrollText className="text-blue-600 w-8 h-8 mb-2" />
            <h2 className="text-xl font-semibold text-blue-800 mb-2">Mis casos</h2>
            <p className="text-gray-600">Gestiona las situaciones, personas o cosas que has decidido perdonar.</p>
          </CardContent>
        </Card>
      </div>

      <Button className="mt-10 bg-blue-600 text-white px-6 py-3 rounded-2xl shadow hover:bg-blue-700 transition">
        Empezar ahora
      </Button>
    </div>
  );
}

export default function ForgivenessApp() {
  const [showWelcome, setShowWelcome] = React.useState(true);

  return showWelcome ? (
    <WelcomeScreen onContinue={() => setShowWelcome(false)} />
  ) : (
    <MainAppContent />
  );
}
