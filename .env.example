# Example env file for local development tools
# Note: The app itself does not use these unless you wire them in server code.

# Anthropic API key for Claude Code (VS Code) if you prefer environment variables.
# Typically you set this inside VS Code Settings instead of here.
ANTHROPIC_API_KEY=

# Optional: If you use OpenRouter as provider for Claude models
# OPENROUTER_API_KEY=

